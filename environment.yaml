name: ctop
channels:
  - conda-forge
dependencies:
  # Python and scientific libraries
  - python=3.10
  - h5py
  - scipy
  - matplotlib
  - pandas
  - uproot
  - pyyaml        # required by wrapper/main.py (yaml-cpp is C++, not python)
  - seaborn
  
  # General libraries and tools
  - parallel
  - gxx=13.3.0  # Compiler
  - gcc=13.3.0
  - openmpi
  - hdf5
  - zlib
  - doxygen
  - gsl=2.7        # was 2.7.1: that patch version is not on conda-forge (only the defaults channel)
  - cmake
  - eigen=3.4.0
  - yaml-cpp
  
  # Boost (specific version)
  - boost=1.78.0
  - boost-cpp=1.78.0
  
  # ROOT (no GUI/display dependencies needed)
  - root_base
  - ca-certificates
  - openssl
