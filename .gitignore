tmp/*
tmp
results/*
results
tables
tables/*
*.pyc
*.db
analysis/build
models/decays/*.o
wrapper/external_packages
.vscode

# Editor / tooling
.claude/
.ipynb_checkpoints/

# Build artifacts
*.o
utils/sample_dijets/sample_dijets

# CCAKE/AMPTGenesis write a debug.root relative to the wrapper's CWD (the
# repo root, per the README quickstart) rather than the per-event output dir
debug.root

# Run outputs (per-run scratch, not versioned)
db_*/
logs/
log*.dat

# Per-run generated configs and event lists
tmp_*.yaml
*_event_list.txt
repro_jets_tiny/
