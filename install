#!/usr/bin/env bash
# Kept for backward compatibility: the install logic now lives in ./chain and
# the idempotent scripts under setup/. See README for details.
exec "$(dirname "${BASH_SOURCE[0]}")/chain" install "$@"
